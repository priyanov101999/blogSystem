<!DOCTYPE html>
<head><title>Blogs</title>
	
	<style>
			form{
         width:50%;
          }
.form-input{
            display: inline-block;
			width: 100%;
			height: 2rem;
			margin: 0.5rem 1rem;
		}
.card{
     margin:auto;
     width: 50%;
     
     border-radius: 5px;
     border: 3px solid black;
     
     display: inline-block;
     padding: 0.25rem 1rem;
     margin: 0.25rem 1rem;
		}
.btn {
     
     padding: 0.25rem 1rem;
     text-decoration: none;
     font: inherit;
     border: 1px solid #00695c;
     color: #00695c;
     background: white;
     border-radius: 3px;
     cursor: pointer;
     float:right;
     margin: 0.5rem 1rem;
}
.title{
	color: red;
	font-weight: bold;
	display: block;
}
.description{
	color: black;
	font-family: "Times New Roman";
	display: block;
}
.author{
	font-style: italic;
	display: block;
}

	</style>
	</head>
	<body >
		
		<a href="/add-blog" class="btn">Create Blog</a>
		<%  if(blogs.length > 0){ %>
		<div >
			<% for(let blog of blogs) {%>
			<div class="card">
				<label  class="title"><%= blog.title %></label>
				<label class="description"><%= blog.description %></label>
				<label class="author"><%= blog.author %></label>
				<a href="/edit-blog/<%= blog.id%>" class="btn">EDIT</a>
				<a href="/delete-blog/<%= blog.id%>" class="btn">DELETE</a>
			</div>
			<% } %>
		</div>
		<% }else{%>
<div>No Blogs are there to show</div>
		<%} %>
		<form method="post" action="/author-search">
			<input class="form-input" type="text" name="search" placeholder="Search by author..." value=""/>
			<input type="submit" value="search" class="btn">
		</form>
	</body>
	</html>